<!DOCTYPE html>
<html>
  <head>
    <title>Icapp</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <div class="wrap">
  <header>
  <div class="container">    
    <ul class="row">
      <% if logged_in? %>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to '投稿',new_ic_path%></li>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to "Profile", user_path(current_user.id) %></li>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to '投稿一覧',ics_path%></li>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to 'お気に入り一覧',favorite_path(current_user.id)%></li>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
    <% else %>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to "Sign up", new_user_path %></li>
        <li class="col-xs-2 col-sm-2 col-md-2"><%= link_to "Login", new_session_path %></li>
    <% end %>
    </ul>
  </div>
  </header>
  
  <body>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    
    <%= yield %>
  </body>
  
   <footer>
    <p>Icapp</p>
    <p>© Naoto Eguchi</p>
   </footer>
  </div>
</html>
